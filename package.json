{
  "dependencies": {
    "papaparse": "^5.5.2"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "name": "awqiem-revised",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d pages",
    "build": "mkdir -p pages/css pages/js && cp -r css/* pages/css/ && cp -r js/* pages/js/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Air and Water Quality Index and Environmental Monitoring System"
}
